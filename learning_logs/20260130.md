# learning_logs 기준 vs code에서 열기
.md파일은 vs code, python은 pycharm으로 한다.

pycharm에서 github repository로 올릴 수 있도록 설정
git init
git config user.name "깃허브아이디"
git confit user.email "e메일"

```python
def turn_right():
    turn_left()
    turn_left()
    turn_left()

def jump():
    move()
    turn_left()
    move()
    turn_right()
    move()
    turn_right()
    move()
    turn_left()

for i in range(6):
    jump()
```

```python
def turn_right():
    turn_left()
    turn_left()
    turn_left()

while(not at_goal()):
    if right_is_clear():
        turn_right()
        if front_is_clear():
            move()
    elif front_is_clear() and wall_on_right():
        move()
    else:
        turn_left()

# 우수법
```